import RegisterCon from "../../src/components/units/board/write/BoardWrite.container";

export default function RegisterPage() {
    return <RegisterCon />;
}
// 페이지의 권한여부를 주어 질때 다시 사용 (ex => 마이페이지. 전체 게시판)
